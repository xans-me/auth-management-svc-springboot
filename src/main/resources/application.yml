server:
  port: 8080

spring:
  application:
    name: auth-service
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5432/auth-management
    username: xans
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false

#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka
#    fetch-registry: true
#    register-with-eureka: true